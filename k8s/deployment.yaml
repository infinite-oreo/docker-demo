apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-node
spec:
  replicas: 3
  selector:
    matchLabels:
      app: health-node
  template:
    metadata:
      labels:
        app: health-node
    spec:
      containers:
        - name: health-node
          image: health-node:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: NODE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
            failureThreshold: 3